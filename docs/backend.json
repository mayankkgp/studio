{
  "entities": {
    "OrderDraft": {
      "title": "Order Draft",
      "description": "Schema for orders saved locally. Used for client-side type reference.",
      "type": "object",
      "properties": {
        "orderId": { "type": "string" },
        "id": { "type": "string" },
        "eventDetails": { "type": "object" },
        "deliverables": { "type": "array" },
        "paymentReceived": { "type": "number" },
        "currentStep": { "type": "string" },
        "lastSavedAt": { "type": "string" }
      },
      "required": ["orderId"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  }
}
