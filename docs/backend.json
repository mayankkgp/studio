
{
  "entities": {
    "OrderDraft": {
      "title": "Order Draft",
      "description": "A partially completed order saved for later completion in the quote building process with metadata for tracking.",
      "type": "object",
      "properties": {
        "orderId": { "type": "string" },
        "eventDetails": { "type": "object" },
        "deliverables": { "type": "array" },
        "paymentReceived": { "type": "number" },
        "currentStep": { "type": "string" },
        "lastSavedAt": { "type": "object", "description": "Firestore Server Timestamp" }
      },
      "required": ["orderId", "eventDetails"]
    },
    "ActiveOrder": {
      "title": "Active Order",
      "description": "A finalized and activated order ready for fulfillment with activation metadata.",
      "type": "object",
      "properties": {
        "orderId": { "type": "string" },
        "eventDetails": { "type": "object" },
        "deliverables": { "type": "array" },
        "paymentReceived": { "type": "number" },
        "activatedAt": { "type": "object", "description": "Firestore Server Timestamp" }
      },
      "required": ["orderId", "eventDetails"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/drafts/{orderId}": {
      "schema": "OrderDraft",
      "description": "Secure store for partially completed order drafts."
    },
    "/active-orders/{orderId}": {
      "schema": "ActiveOrder",
      "description": "Secure store for confirmed and activated orders."
    }
  }
}
