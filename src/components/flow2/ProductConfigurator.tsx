'use client';

/**
 * DEPRECATED: This component has been replaced by the inline DeliverableRow system.
 * Modals are no longer used for product configuration to support high-speed quote building.
 */
export function ProductConfigurator() {
    return null;
}
